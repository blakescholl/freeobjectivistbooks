<% if @fulfillment_count > 0 %>
<p>You've sent <%= @fulfillment_count %> books in the last week--thanks!</p>
<% end %>

<p>
  There <%= @single ? "is" : "are" %> currently <%= pluralize @outstanding_count, 'donation' %> waiting to be fulfilled
  on Free Objectivist Books. See and send <%= @single ? "it" : "them" %> using this link:
</p>

<p><strong><%= link_to "Help out by sending books", volunteer_url %></strong></p>

<% if @fulfillment_count <= 0 %>
<p>(You haven't sent any books in the last week, by the way.)</p>
<% end %>
